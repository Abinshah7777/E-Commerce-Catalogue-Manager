# Core Flask and extensions
Flask==2.3.2
Werkzeug==2.3.2
Jinja2==3.1.2
itsdangerous==2.1.2
MarkupSafe==2.1.3

# Swagger support via Flask-RESTX
flask-restx==1.1.0

# MySQL database connector
mysql-connector-python==8.3.0

# Date parsing and validation
python-dateutil==2.8.2

# Unit testing
pytest==8.2.1

# Logging (standard library, no external dependency)
